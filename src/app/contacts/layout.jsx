import React from 'react'
export const metadata = {
    title: 'this my contact page',
    description: 'contact page',
  }
export default function layout({children}) {
  return (
    <div>layout for contacts</div>
  )
}
