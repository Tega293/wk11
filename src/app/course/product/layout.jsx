import React from 'react'
export const metadata = {
    title: 'this is my product design page',
    description: 'product design page description',
  }
export default function layout({children}) {
  return (
    <div>{children}</div>
  )
}
