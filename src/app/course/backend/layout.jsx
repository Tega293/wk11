import React from 'react'
export const metadata = {
    title: 'backend page',
    description: 'this is my backend page',
  }
export default function layout({children,}) {
  return (
    <div> backend layout</div>
  )
}
